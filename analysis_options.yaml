include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.gen.dart"
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    missing_required_param: error
    missing_return: error
    todo: ignore

linter:
  rules:
    # ── Formatting & Style ────────────────────────────────────────────────────
    curly_braces_in_flow_control_structures: true   # if/for/while always use {}
    # Forces the body onto its own line so single-line `if (x) return;` is not
    # allowed — combined with the rule above, every flow control body must use {}.
    always_put_control_body_on_new_line: true
    prefer_single_quotes: true
    require_trailing_commas: true
    eol_at_end_of_file: true

    # ── Types & Declarations ──────────────────────────────────────────────────
    always_declare_return_types: true
    avoid_dynamic_calls: true
    avoid_annotating_with_dynamic: true

    # ── Functions & Parameters ────────────────────────────────────────────────
    always_put_required_named_parameters_first: true
    avoid_positional_boolean_parameters: true
    use_named_constants: true

    # ── Variables ─────────────────────────────────────────────────────────────
    prefer_final_fields: true
    prefer_final_in_for_each: true
    prefer_final_locals: true

    # ── Async ─────────────────────────────────────────────────────────────────
    unawaited_futures: true
    unnecessary_await_in_return: true

    # ── Classes & Constructors ────────────────────────────────────────────────
    use_super_parameters: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true

    # ── Flutter-specific ──────────────────────────────────────────────────────
    sort_child_properties_last: true
    use_colored_box: true
    use_decorated_box: true

    # ── Error Handling ────────────────────────────────────────────────────────
    avoid_catches_without_on_clauses: true
    use_rethrow_when_possible: true
    only_throw_errors: true

    # ── Code Quality ──────────────────────────────────────────────────────────
    no_literal_bool_comparisons: true
    avoid_print: true
    avoid_redundant_argument_values: true
    avoid_unused_constructor_parameters: true
    cancel_subscriptions: true
    close_sinks: true
    literal_only_boolean_expressions: true
    no_adjacent_strings_in_list: true
    prefer_asserts_with_message: true
    throw_in_finally: true
    unnecessary_statements: true
